import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://Huzair43.github.io",
  base: "/genZ-africa/",
  markdown: {
    shikiConfig: {
      theme: "github-light"
    }
  }
});
